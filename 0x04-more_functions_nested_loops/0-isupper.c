#include "main.h"

/**
* _isupper - check the code.
* @letter: the letter to test
* Return: Always 0.
*/

int _isupper(int letter)
{
if (letter >= 'A' && letter <= 'Z')
{
return (1);
}

else
{
return (0);
}
}
