#include 'main.h'
#include <stdio.h>

/**
* mul - multiply two numbers
* @num1: first number
* @num2: second number
*
* Return: Returns the product of the two num
*/

int mul(int num1, int num2)
{
int mul_val;
mul_val = num1 * num2;
return (mul_val);
}
